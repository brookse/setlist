import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lyzzibrooks.setlist',
  appName: 'setlist',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
